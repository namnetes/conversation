Expliquer pourquoi il y a des erreurs d'affichage des caractères accentués apparaissent lorsuqe le programme java s'execute sur un système IBM Mainframe sous USS ou l'encodage par défaut est IBM-1047.
Attention le programme est exécuter depuis une fenêtre powershelle sous windows 10 via ssh ainsi

PS C:\Users\MARCHAND_A\IDzPortable\Workspace\HashCalculator\bin> ssh toto@dev.prodinfo.fr.cly "/usr/lpp/java/current_64/bin/java MD5Hasher"
emarcha@dev.prodinfo.fr.cly's password:
Original String: Vous voulez confirmer qu'un fichier a �t� re�u intact ? Comparez les hachages de l'exp�diteur et du destinataire.
Hash Value: B7542AFC010B783636A225E34479EC8E

Le source java est là

import java.nio.charset.Charset;
import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;

public class MD5Hasher {

    public static void main(String[] args) {

    	String inputString = "Vous voulez confirmer qu'un fichier a été reçu intact ? Comp"
        		           + "arez les hachages de l'expéditeur et du destinataire.";

        String hashValue = calculateHash(inputString);
        System.out.println("Original String: " + inputString);
        System.out.println("Hash Value: " + hashValue.toUpperCase());
    }

    private static String calculateHash(String input) {
        try {
            MessageDigest digest = MessageDigest.getInstance("MD5");
            byte[] encodedHash = digest.digest(input.getBytes(Charset.forName("IBM1147")));

            StringBuilder hexString = new StringBuilder(2 * encodedHash.length);
            for (byte b : encodedHash) {
                String hex = Integer.toHexString(0xff & b);
                if (hex.length() == 1) {
                    hexString.append('0');
                }
                hexString.append(hex);
            }

            return hexString.toString();
        } catch (NoSuchAlgorithmException e) {
            e.printStackTrace();
            return null;
        }
    }
}
