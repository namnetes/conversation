import socks
import socket

# Sauvegardez les param̬tres du socket par d̩faut
default_socket = socket.socket

# Configurez le proxy avec PySocks
socks.setdefaultproxy(socks.PROXY_TYPE_HTTP, 'your_proxy_host', your_proxy_port, True, 'your_username', 'your_password')
socket.socket = socks.socksocket

# ... faire quelque chose avec le socket modifi̩ ...

# Restaurez les param̬tres du socket par d̩faut
socket.socket = default_socket